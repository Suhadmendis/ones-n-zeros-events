<?php
// payroll_periods_data.php — generated by Generate Module

require_once __DIR__ . '/../../../../server/supabase.php';
require_once __DIR__ . '/../../../../server/session.php';
require_once __DIR__ . '/../../../../server/general/number_series.php';

header('Content-Type: application/json');

function savePayrollPeriods(array $data): array {
    $code = trim($data['code'] ?? '');
    $existing = supabase_get(SB_API . 'm_payroll_periods?select=id&code=eq.' . urlencode($code) . '&limit=1');
    if (!empty($existing)) {
        http_response_code(409);
        return ['error' => "Code '{$code}' already exists."];
    }

    $ref = consumeNextReference('payroll_periods');

    return supabase_post(SB_API . 'm_payroll_periods', [
        'ref' => $ref,
        'payroll_profile_ref' => !empty($data['payroll_profile_ref']) ? $data['payroll_profile_ref'] : null,
        'code' => $data['code'] ?? '',
        'name' => $data['name'] ?? '',
        'start_date' => !empty($data['start_date']) ? $data['start_date'] : null,
        'end_date' => !empty($data['end_date']) ? $data['end_date'] : null,
        'payment_date' => !empty($data['payment_date']) ? $data['payment_date'] : null,
        'period_status' => $data['period_status'] ?? '',
        'created_by' => current_user()['ref'] ?? null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function updatePayrollPeriods(array $data): array {
    $ref = trim($data['ref'] ?? '');
    if ($ref === '' || !recordExists('m_payroll_periods', $ref)) {
        return ['error' => 'Record not found.'];
    }

    return supabase_patch(SB_API . 'm_payroll_periods?ref=eq.' . urlencode($ref), [
        'payroll_profile_ref' => !empty($data['payroll_profile_ref']) ? $data['payroll_profile_ref'] : null,
        'code' => $data['code'] ?? '',
        'name' => $data['name'] ?? '',
        'start_date' => !empty($data['start_date']) ? $data['start_date'] : null,
        'end_date' => !empty($data['end_date']) ? $data['end_date'] : null,
        'payment_date' => !empty($data['payment_date']) ? $data['payment_date'] : null,
        'period_status' => $data['period_status'] ?? '',
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function listPayrollPeriods(): array {
    return supabase_get(SB_API . 'm_payroll_periods?select=id,ref,payroll_profile_ref,code,name,start_date,end_date,payment_date,period_status,m_payroll_profiles(name)&order=id.asc');
}

$action = $_GET['action'] ?? '';

if ($action === 'list') {
    echo json_encode(listPayrollPeriods());

} elseif ($action === 'save') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(savePayrollPeriods($body));

} elseif ($action === 'update') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(updatePayrollPeriods($body));

} elseif ($action === 'exists') {
    $ref = $_GET['ref'] ?? '';
    echo json_encode(['exists' => $ref !== '' && recordExists('m_payroll_periods', $ref)]);

} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}