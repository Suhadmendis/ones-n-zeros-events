// salary_components_search.js — generated by Generate Module

$(document).ready(function () {

  const table = $('#salaryComponentsSearchTable').DataTable({
    ajax: { url: '/modules/human_resources_payroll/salary_setup/salary_components/salary_components_data.php?action=list', dataSrc: '' },
    columns: [
      { data: 'ref' },
      { data: 'code' },
      { data: 'name' },
      { data: 'description' },
      { data: 'component_type' },
      { data: 'calculation_type' },
      { data: 'is_taxable' },
      { data: 'is_recurring' },
      { data: 'affects_gross_salary' },
      { data: 'affects_net_salary' },
      { data: 'sort_order' },
    ],
    pageLength: 10,
    lengthMenu: [5, 10, 25, 50],
    dom: '<"d-flex justify-content-between align-items-center mb-2"Bf>rtip',
    buttons: [
      { extend: 'copy',  className: 'btn btn-sm btn-secondary' },
      { extend: 'excel', className: 'btn btn-sm btn-success',  text: '<i class="bi bi-file-earmark-excel me-1"></i>Excel' },
      { extend: 'csv',   className: 'btn btn-sm btn-info',     text: '<i class="bi bi-filetype-csv me-1"></i>CSV' },
      { extend: 'pdf',   className: 'btn btn-sm btn-danger',   text: '<i class="bi bi-file-earmark-pdf me-1"></i>PDF' },
      { extend: 'print', className: 'btn btn-sm btn-secondary' },
    ],
    language: {
      search: 'Global search:',
      lengthMenu: 'Show _MENU_ entries',
      info: 'Showing _START_ to _END_ of _TOTAL_ Salary Components records',
    },
  });

  $('#salaryComponentsSearchTable tbody').on('click', 'tr', function () {
    const data = table.row(this).data();
    if (!data) return;
    bootstrap.Modal.getInstance(document.getElementById('salaryComponentsSearchModal')).hide();
    document.dispatchEvent(new CustomEvent('salary_components-selected', { detail: data }));
  });

  $('#salaryComponentsSearchModal').on('shown.bs.modal', function () {
    table.ajax.reload(null, false);
    table.columns.adjust();
  });

});