// salary_components.js — generated by Generate Module

const { createApp } = Vue;

createApp({
  data() {
    return {
      systemName: SYSTEM_NAME,
      title:      '',
      loading: false,
      saving:  false,
      saved:   false,
      error:   '',
      isExisting: false,
      form: {
        ref: '',
      code: '',
      name: '',
      description: '',
      component_type: '',
      calculation_type: '',
      is_taxable: false,
      is_recurring: true,
      affects_gross_salary: true,
      affects_net_salary: true,
      sort_order: 0,
      },
    };
  },

  computed: {
    isDirty() {
      return (
        this.form.code !== '' ||
        this.form.name !== '' ||
        this.form.description !== '' ||
        this.form.component_type !== '' ||
        this.form.calculation_type !== '' ||
        this.form.is_taxable !== false ||
        this.form.is_recurring !== true ||
        this.form.affects_gross_salary !== true ||
        this.form.affects_net_salary !== true ||
        this.form.sort_order !== 0
      );
    },
  },

  mounted() {
    this.fetchRefNumber();
    document.addEventListener('salary_components-selected', (e) => this.loadRecord(e.detail));
  },

  methods: {
    fetchRefNumber() {
      this.loading = true;
      axios.get('/server/general/module_data.php?system_name=' + this.systemName)
        .then(res => { this.form.ref = res.data.ref; this.title = res.data.module; })
        .catch(err => { this.error = 'Failed to load reference number.'; console.error(err); })
        .finally(() => { this.loading = false; });
    },

    onAdd()    { this.onReset(); this.fetchRefNumber(); },
    onPrint() {
      if (!this.form.ref) { this.error = 'No record to print. Search and select a saved record first.'; return; }
      axios.get('/modules/human_resources_payroll/salary_setup/salary_components/salary_components_data.php?action=exists&ref=' + encodeURIComponent(this.form.ref))
        .then(res => {
          if (!res.data.exists) {
            this.error = 'No saved record found for this reference. Please search and select a record to print.';
            return;
          }
          const params = new URLSearchParams({
            ref: this.form.ref,
        code: String(this.form.code ?? ''),
        name: String(this.form.name ?? ''),
        description: String(this.form.description ?? ''),
        component_type: String(this.form.component_type ?? ''),
        calculation_type: String(this.form.calculation_type ?? ''),
        is_taxable: String(this.form.is_taxable ?? ''),
        is_recurring: String(this.form.is_recurring ?? ''),
        affects_gross_salary: String(this.form.affects_gross_salary ?? ''),
        affects_net_salary: String(this.form.affects_net_salary ?? ''),
        sort_order: String(this.form.sort_order ?? ''),
          });
          window.open('/modules/human_resources_payroll/salary_setup/salary_components/salary_components_print.php?' + params.toString(), '_blank');
        })
        .catch(err => { this.error = 'Failed to verify record.'; console.error(err); });
    },
    onCancel() { this.onReset(); },
    onClose()  { this.onReset(); },

    onReset() {
      this.saved = false;
      this.error = '';
      this.isExisting = false;
      this.form.code = '';
      this.form.name = '';
      this.form.description = '';
      this.form.component_type = '';
      this.form.calculation_type = '';
      this.form.is_taxable = false;
      this.form.is_recurring = true;
      this.form.affects_gross_salary = true;
      this.form.affects_net_salary = true;
      this.form.sort_order = 0;
    },

    onSave() {
      if (!this.isDirty) return;
      if (this.$refs.form && !this.$refs.form.reportValidity()) return;
      this.saving = true;
      this.saved  = false;
      this.error  = '';

      const proceed = (action) => {
        axios.post('/modules/human_resources_payroll/salary_setup/salary_components/salary_components_data.php?action=' + action, this.form)
          .then(() => {
            this.saved = true;
            this.onReset();
            this.fetchRefNumber();
          })
          .catch(err => { this.error = err.response?.data?.error || 'Failed to save.'; console.error(err); })
          .finally(() => { this.saving = false; });
      };

      if (this.isExisting) {
        axios.get('/modules/human_resources_payroll/salary_setup/salary_components/salary_components_data.php?action=exists&ref=' + encodeURIComponent(this.form.ref))
          .then(res => {
            if (!res.data.exists) { this.error = 'Record not found. Please search again.'; this.saving = false; return; }
            proceed('update');
          })
          .catch(err => { this.error = 'Failed to verify record.'; console.error(err); this.saving = false; });
      } else {
        proceed('save');
      }
    },

    loadRecord(data) {
      this.form.ref = data.ref;
      this.form.code = data.code;
      this.form.name = data.name;
      this.form.description = data.description;
      this.form.component_type = data.component_type;
      this.form.calculation_type = data.calculation_type;
      this.form.is_taxable = data.is_taxable;
      this.form.is_recurring = data.is_recurring;
      this.form.affects_gross_salary = data.affects_gross_salary;
      this.form.affects_net_salary = data.affects_net_salary;
      this.form.sort_order = data.sort_order;
      this.isExisting = true;
      this.saved = false;
      this.error = '';
    },
  },
}).mount('#salary-components-app');