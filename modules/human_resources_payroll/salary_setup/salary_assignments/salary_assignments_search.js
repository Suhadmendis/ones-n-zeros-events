// salary_assignments_search.js — generated by Generate Module

$(document).ready(function () {

  // ── Employment Record picker ─────────────────────────────────────────────
  const employmentRecordPicker = $('#saEmploymentRecordPickerTable').DataTable({
    ajax: { url: '/modules/master_files/people/employment_record/employment_record_data.php?action=list', dataSrc: '' },
    columns: [
      { data: 'ref' },
      { data: 'm_employees.full_name', defaultContent: '' },
      { data: 'm_employment_types.name', defaultContent: '' },
    ],
    pageLength: 10,
    searching: true,
    dom: '<"d-flex justify-content-between align-items-center mb-2"f>rtip',
    language: { search: 'Filter:', info: 'Showing _START_ to _END_ of _TOTAL_' },
  });

  $('#saEmploymentRecordPickerTable tbody').on('click', 'tr', function () {
    const data = employmentRecordPicker.row(this).data();
    if (!data) return;
    bootstrap.Modal.getInstance(document.getElementById('saEmploymentRecordPickerModal')).hide();
    document.dispatchEvent(new CustomEvent('sa-employment-record-selected', { detail: data }));
  });

  $('#saEmploymentRecordPickerModal').on('shown.bs.modal', function () {
    employmentRecordPicker.ajax.reload(null, false);
    employmentRecordPicker.columns.adjust();
  });

  // ── Currency picker ──────────────────────────────────────────────────────
  const currencyPicker = $('#saCurrencyPickerTable').DataTable({
    ajax: { url: '/modules/finance/assets_budgeting/multi_currency/multi_currency_data.php?action=list', dataSrc: '' },
    columns: [
      { data: 'ref' },
      { data: 'currency_code' },
      { data: 'currency_name' },
    ],
    pageLength: 10,
    searching: true,
    dom: '<"d-flex justify-content-between align-items-center mb-2"f>rtip',
    language: { search: 'Filter:', info: 'Showing _START_ to _END_ of _TOTAL_' },
  });

  $('#saCurrencyPickerTable tbody').on('click', 'tr', function () {
    const data = currencyPicker.row(this).data();
    if (!data) return;
    bootstrap.Modal.getInstance(document.getElementById('saCurrencyPickerModal')).hide();
    document.dispatchEvent(new CustomEvent('sa-currency-selected', { detail: data }));
  });

  $('#saCurrencyPickerModal').on('shown.bs.modal', function () {
    currencyPicker.ajax.reload(null, false);
    currencyPicker.columns.adjust();
  });

  // ── Salary Component picker ──────────────────────────────────────────────
  const salaryComponentPicker = $('#saSalaryComponentPickerTable').DataTable({
    ajax: { url: '/modules/human_resources_payroll/salary_setup/salary_components/salary_components_data.php?action=list', dataSrc: '' },
    columns: [
      { data: 'ref' },
      { data: 'code' },
      { data: 'name' },
      { data: 'component_type', defaultContent: '' },
    ],
    pageLength: 10,
    searching: true,
    dom: '<"d-flex justify-content-between align-items-center mb-2"f>rtip',
    language: { search: 'Filter:', info: 'Showing _START_ to _END_ of _TOTAL_' },
  });

  $('#saSalaryComponentPickerTable tbody').on('click', 'tr', function () {
    const data = salaryComponentPicker.row(this).data();
    if (!data) return;
    bootstrap.Modal.getInstance(document.getElementById('saSalaryComponentPickerModal')).hide();
    document.dispatchEvent(new CustomEvent('sa-component-selected', { detail: data }));
  });

  $('#saSalaryComponentPickerModal').on('shown.bs.modal', function () {
    salaryComponentPicker.ajax.reload(null, false);
    salaryComponentPicker.columns.adjust();
  });

  const table = $('#salaryAssignmentsSearchTable').DataTable({
    ajax: { url: '/modules/human_resources_payroll/salary_setup/salary_assignments/salary_assignments_data.php?action=list', dataSrc: '' },
    columns: [
      { data: 'ref' },
      { data: 'm_employment_records.m_employees.full_name', defaultContent: '' },
      { data: 'm_currencies.currency_code', defaultContent: '' },
      { data: 'effective_from' },
      { data: 'effective_to' },
      { data: 'remarks' },
    ],
    pageLength: 10,
    lengthMenu: [5, 10, 25, 50],
    dom: '<"d-flex justify-content-between align-items-center mb-2"Bf>rtip',
    buttons: [
      { extend: 'copy',  className: 'btn btn-sm btn-secondary' },
      { extend: 'excel', className: 'btn btn-sm btn-success',  text: '<i class="bi bi-file-earmark-excel me-1"></i>Excel' },
      { extend: 'csv',   className: 'btn btn-sm btn-info',     text: '<i class="bi bi-filetype-csv me-1"></i>CSV' },
      { extend: 'pdf',   className: 'btn btn-sm btn-danger',   text: '<i class="bi bi-file-earmark-pdf me-1"></i>PDF' },
      { extend: 'print', className: 'btn btn-sm btn-secondary' },
    ],
    language: {
      search: 'Global search:',
      lengthMenu: 'Show _MENU_ entries',
      info: 'Showing _START_ to _END_ of _TOTAL_ Salary Assignments records',
    },
  });

  $('#salaryAssignmentsSearchTable tbody').on('click', 'tr', function () {
    const data = table.row(this).data();
    if (!data) return;
    bootstrap.Modal.getInstance(document.getElementById('salaryAssignmentsSearchModal')).hide();
    document.dispatchEvent(new CustomEvent('salary_assignments-selected', { detail: data }));
  });

  $('#salaryAssignmentsSearchModal').on('shown.bs.modal', function () {
    table.ajax.reload(null, false);
    table.columns.adjust();
  });

});