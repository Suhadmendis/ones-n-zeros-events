<?php
// maintenance_type_data.php — generated by Generate Module

require_once __DIR__ . '/../../../../server/supabase.php';
require_once __DIR__ . '/../../../../server/session.php';
require_once __DIR__ . '/../../../../server/general/number_series.php';

header('Content-Type: application/json');

function saveMaintenanceType(array $data): array {
    $ref = consumeNextReference('maintenance_type');

    return supabase_post(SB_API . 'm_maintenance_types', [
        'ref' => $ref,
        'code' => $data['code'] ?? '',
        'name' => $data['name'] ?? '',
        'description' => $data['description'] ?? '',
        'service_interval_km' => $data['service_interval_km'] ?? 0,
        'service_interval_days' => $data['service_interval_days'] ?? 0,
        'created_by' => current_user()['ref'] ?? null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function updateMaintenanceType(array $data): array {
    $ref = trim($data['ref'] ?? '');
    if ($ref === '' || !recordExists('m_maintenance_types', $ref)) {
        return ['error' => 'Record not found.'];
    }

    return supabase_patch(SB_API . 'm_maintenance_types?ref=eq.' . urlencode($ref), [
        'code' => $data['code'] ?? '',
        'name' => $data['name'] ?? '',
        'description' => $data['description'] ?? '',
        'service_interval_km' => $data['service_interval_km'] ?? 0,
        'service_interval_days' => $data['service_interval_days'] ?? 0,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function listMaintenanceType(): array {
    return supabase_get(SB_API . 'm_maintenance_types?select=id,ref,code,name,description,service_interval_km,service_interval_days&order=id.asc');
}

$action = $_GET['action'] ?? '';

if ($action === 'list') {
    echo json_encode(listMaintenanceType());

} elseif ($action === 'save') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(saveMaintenanceType($body));

} elseif ($action === 'update') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(updateMaintenanceType($body));

} elseif ($action === 'exists') {
    $ref = $_GET['ref'] ?? '';
    echo json_encode(['exists' => $ref !== '' && recordExists('m_maintenance_types', $ref)]);

} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}