<?php
// quotation_notes_templates_data.php — generated by Generate Module

require_once __DIR__ . '/../../../../server/supabase.php';
require_once __DIR__ . '/../../../../server/session.php';
require_once __DIR__ . '/../../../../server/general/number_series.php';

header('Content-Type: application/json');

function saveQuotationNotesTemplates(array $data): array {
    $ref = consumeNextReference('quotation_notes_templates');

    $record = supabase_post(SB_API . 'm_quotation_notes_templates', [
        'ref' => $ref,
        'name' => $data['name'] ?? '',
        'description' => $data['description'] ?? '',
        'created_by' => current_user()['ref'] ?? null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);

    return $record;
}

function updateQuotationNotesTemplates(array $data): array {
    $ref = trim($data['ref'] ?? '');
    if ($ref === '' || !recordExists('m_quotation_notes_templates', $ref)) {
        return ['error' => 'Record not found.'];
    }

    return supabase_patch(SB_API . 'm_quotation_notes_templates?ref=eq.' . urlencode($ref), [
        'name' => $data['name'] ?? '',
        'description' => $data['description'] ?? '',
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function listQuotationNotesTemplates(): array {
    return supabase_get(SB_API . 'm_quotation_notes_templates?select=id,ref,name,description&order=id.asc');
}

$action = $_GET['action'] ?? '';

if ($action === 'list') {
    echo json_encode(listQuotationNotesTemplates());

} elseif ($action === 'save') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(saveQuotationNotesTemplates($body));

} elseif ($action === 'update') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(updateQuotationNotesTemplates($body));

} elseif ($action === 'exists') {
    $ref = $_GET['ref'] ?? '';
    echo json_encode(['exists' => $ref !== '' && recordExists('m_quotation_notes_templates', $ref)]);

} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}