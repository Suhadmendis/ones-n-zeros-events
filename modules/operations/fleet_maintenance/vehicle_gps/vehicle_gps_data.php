<?php
// vehicle_gps_data.php — generated by Generate Module

require_once __DIR__ . '/../../../../server/supabase.php';
require_once __DIR__ . '/../../../../server/session.php';
require_once __DIR__ . '/../../../../server/general/number_series.php';

header('Content-Type: application/json');

function saveVehicleGps(array $data): array {
    $ref = consumeNextReference('vehicle_gps');

    return supabase_post(SB_API . 't_vehicle_gps', [
        'ref' => $ref,
        'vehicle_ref' => !empty($data['vehicle_ref']) ? $data['vehicle_ref'] : null,
        'gps_device_ref' => !empty($data['gps_device_ref']) ? $data['gps_device_ref'] : null,
        'installed_date' => !empty($data['installed_date']) ? $data['installed_date'] : null,
        'removed_date' => !empty($data['removed_date']) ? $data['removed_date'] : null,
        'created_by' => current_user()['ref'] ?? null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function updateVehicleGps(array $data): array {
    $ref = trim($data['ref'] ?? '');
    if ($ref === '' || !recordExists('t_vehicle_gps', $ref)) {
        return ['error' => 'Record not found.'];
    }

    return supabase_patch(SB_API . 't_vehicle_gps?ref=eq.' . urlencode($ref), [
        'vehicle_ref' => !empty($data['vehicle_ref']) ? $data['vehicle_ref'] : null,
        'gps_device_ref' => !empty($data['gps_device_ref']) ? $data['gps_device_ref'] : null,
        'installed_date' => !empty($data['installed_date']) ? $data['installed_date'] : null,
        'removed_date' => !empty($data['removed_date']) ? $data['removed_date'] : null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function listVehicleGps(): array {
    return supabase_get(SB_API . 't_vehicle_gps?select=id,ref,vehicle_ref,gps_device_ref,installed_date,removed_date,m_vehicles(ref,plate_number),m_gps_devices(code,serial_number)&order=id.asc');
}

$action = $_GET['action'] ?? '';

if ($action === 'list') {
    echo json_encode(listVehicleGps());

} elseif ($action === 'save') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(saveVehicleGps($body));

} elseif ($action === 'update') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(updateVehicleGps($body));

} elseif ($action === 'exists') {
    $ref = $_GET['ref'] ?? '';
    echo json_encode(['exists' => $ref !== '' && recordExists('t_vehicle_gps', $ref)]);

} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}