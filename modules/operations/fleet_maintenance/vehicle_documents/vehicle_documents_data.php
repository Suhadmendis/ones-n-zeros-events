<?php
// vehicle_documents_data.php — generated by Generate Module

require_once __DIR__ . '/../../../../server/supabase.php';
require_once __DIR__ . '/../../../../server/session.php';
require_once __DIR__ . '/../../../../server/general/number_series.php';

header('Content-Type: application/json');

function saveVehicleDocuments(array $data): array {
    $ref = consumeNextReference('vehicle_documents');

    return supabase_post(SB_API . 't_vehicle_documents', [
        'ref' => $ref,
        'vehicle_ref' => !empty($data['vehicle_ref']) ? $data['vehicle_ref'] : null,
        'document_type_ref' => !empty($data['document_type_ref']) ? $data['document_type_ref'] : null,
        'document_number' => $data['document_number'] ?? '',
        'issue_date' => !empty($data['issue_date']) ? $data['issue_date'] : null,
        'expiry_date' => !empty($data['expiry_date']) ? $data['expiry_date'] : null,
        'issued_by' => $data['issued_by'] ?? '',
        'attachment' => $data['attachment'] ?? '',
        'remarks' => $data['remarks'] ?? '',
        'created_by' => current_user()['ref'] ?? null,
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function updateVehicleDocuments(array $data): array {
    $ref = trim($data['ref'] ?? '');
    if ($ref === '' || !recordExists('t_vehicle_documents', $ref)) {
        return ['error' => 'Record not found.'];
    }

    return supabase_patch(SB_API . 't_vehicle_documents?ref=eq.' . urlencode($ref), [
        'vehicle_ref' => !empty($data['vehicle_ref']) ? $data['vehicle_ref'] : null,
        'document_type_ref' => !empty($data['document_type_ref']) ? $data['document_type_ref'] : null,
        'document_number' => $data['document_number'] ?? '',
        'issue_date' => !empty($data['issue_date']) ? $data['issue_date'] : null,
        'expiry_date' => !empty($data['expiry_date']) ? $data['expiry_date'] : null,
        'issued_by' => $data['issued_by'] ?? '',
        'attachment' => $data['attachment'] ?? '',
        'remarks' => $data['remarks'] ?? '',
        'updated_by' => current_user()['ref'] ?? null,
    ]);
}

function listVehicleDocuments(): array {
    return supabase_get(SB_API . 't_vehicle_documents?select=id,ref,vehicle_ref,document_type_ref,document_number,issue_date,expiry_date,issued_by,attachment,remarks,m_vehicles(ref,plate_number),m_vehicle_document_types(ref,code,name)&order=id.asc');
}

$action = $_GET['action'] ?? '';

if ($action === 'list') {
    echo json_encode(listVehicleDocuments());

} elseif ($action === 'save') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(saveVehicleDocuments($body));

} elseif ($action === 'update') {
    $body = json_decode(file_get_contents('php://input'), true) ?? [];
    echo json_encode(updateVehicleDocuments($body));

} elseif ($action === 'exists') {
    $ref = $_GET['ref'] ?? '';
    echo json_encode(['exists' => $ref !== '' && recordExists('t_vehicle_documents', $ref)]);

} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}
